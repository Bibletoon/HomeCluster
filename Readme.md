# Home cluster configuration

This repository contains full configuration of my home k3s cluster and some utilities that I use.

* `flux` - directory with all apps configuration, used by [Flux](https://fluxcd.io/) for continious delivery 
* `utils` - directory with different utilities used to configure cluster