apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
    name: joplyn-postgres
    namespace: joplyn
spec:
    instances: 3

    storage:
        storageClass: longhorn-sr
        size: 5Gi
    monitoring:
        enablePodMonitor: true